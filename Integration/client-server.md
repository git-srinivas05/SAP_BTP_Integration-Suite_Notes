# Client Server Architecture

## Definition
A model where a client sends a request and a server responds.

## Why it matters
All APIs and web applications work on this model.

## Real Example
Browser (client) → Google server → Response shown on screen

## Key Points
- Client sends request
- Server processes and responds
- One request → one response
- Uses HTTP protocol
- Always client request first and sever never ask request first
